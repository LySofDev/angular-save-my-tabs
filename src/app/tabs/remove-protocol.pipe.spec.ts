import { RemoveProtocolPipe } from './remove-protocol.pipe';

describe('RemoveProtocolPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveProtocolPipe();
    expect(pipe).toBeTruthy();
  });
});
